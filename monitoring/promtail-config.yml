server:
    http_listen_port: 9080
    grpc_listen_port: 0

positions:
    filename: /tmp/positions.yaml

clients:
    - url: http://loki:3100/loki/api/v1/push

scrape_configs:
    - job_name: erm-dokter-logs
      static_configs:
          - targets:
                - localhost
            labels:
                job: erm-dokter
                app: erm-dokter
                __path__: /var/log/app/*.log
      pipeline_stages:
          - json:
                expressions:
                    level: level
                    app: app
                    msg: msg
                    status: status
                    method: method
                    user: user
          - labels:
                level:
                app:
