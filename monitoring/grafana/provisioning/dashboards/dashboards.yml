apiVersion: 1

providers:
  - name: 'ERM Dokter Dashboards'
    orgId: 1
    folder: 'ERM Dokter'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
